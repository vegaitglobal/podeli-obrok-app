import styled from 'styled-components/native';

const SidebarContainer = styled.Text`
  position: absolute;
  width: 180px;
  height: 561px;
  left: 180px;
  top: 79px;
  background: #f59829;
`;

const SideBarMenu = props => {
  return <SidebarContainer>vuk</SidebarContainer>;
};

export default SideBarMenu;
